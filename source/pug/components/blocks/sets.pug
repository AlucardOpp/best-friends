mixin sets(data)
  section.sets
    .container
      h1.sets__title.title-medium!="Наборы бриллиантов"
    section.sets__wrapper
      h2.visually-hidden!="Наборы"
      ul.sets__list
        each item in data
          li.sets__item
            +set(item)
